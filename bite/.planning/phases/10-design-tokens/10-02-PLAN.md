---
phase: 10-design-tokens
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - resources/css/app.css
  - resources/views/livewire/guest-menu.blade.php
  - resources/views/livewire/pos-dashboard.blade.php
  - resources/views/livewire/shop-settings.blade.php
  - resources/views/livewire/super-admin/shops/index.blade.php
autonomous: false
requirements:
  - DS-01
  - DS-02
must_haves:
  truths:
    - "resources/css/app.css :root block defines --space-1 through --space-12 (12 tokens, mixed-progression on 4px base per D-10)"
    - "Spacing tokens sit AFTER the typography tokens added in Plan 10-01 and BEFORE the closing brace of :root"
    - "Plan 10-01's typography tokens (7 sizes + 4 weights + 6 line-heights) are still present in app.css after this plan runs (regression-checked in Task 1)"
    - "Updated D-26 two-pass literal-value grep across the four verification screens drops from baseline 14 to 0"
    - "Guest menu (resources/views/livewire/guest-menu.blade.php) sweep complete: baseline 5 matches on lines 171, 226, 229, 303, 306 (newly in scope under the two-pass D-26 grep — dashed `margin-right` and packed `gap:4px` values); must drop to 0"
    - "Shop settings (resources/views/livewire/shop-settings.blade.php) sweep complete: baseline 9 matches on lines 25, 28, 47, 50, 54, 62, 70, 76, 102 (one fewer than iteration 1 because the line 43 `padding: 0;` over-match is correctly excluded by the two-pass grep); must drop to 0"
    - "POS dashboard, super-admin shop list each have a baseline of 0 matches under the two-pass D-26 grep — verify still 0 after sweep"
    - "Line 43 in shop-settings.blade.php (the theme-tile button) retains its original `padding: 0; border-radius: 10px; ...` inline style — the two-pass D-26 grep correctly excludes it (no unit on `padding: 0`; `border-radius` not in the prop list), so no edit is needed"
    - "Existing @layer components (.btn-*, .tag, body, h1-h4) remains untouched"
    - "Inline styles outside the four verification screens (KDS, billing, super-admin user management, shared partials) are NOT modified"
    - "Inline styles encoding DYNAMIC values (e.g. `style=\"background-color: {{ $participantColors[...] }}\"`) are preserved as-is — only literal values get tokenized"
    - "Alpine `:style` template literals on shop-settings lines 103/108 are NOT flagged by the two-pass D-26 grep (the grep matches `style=\"...\"`, not `:style=\"...\"`); they are left untouched in Phase 10 and will be picked up by Phase 14"
    - "Guest menu renders correctly in both EN and AR at 360px viewport with the swept token references"
  artifacts:
    - path: "resources/css/app.css"
      provides: "12 spacing tokens inside the existing :root block, after Plan 10-01's typography tokens"
      contains: "--space-1: 4px"
      contains_secondary: "--space-12: 96px"
    - path: "resources/views/livewire/shop-settings.blade.php"
      provides: "Inline `font-size:`, `padding:`, `gap:`, `margin-bottom:`, `margin-top:`, `font-weight:` literal values on 9 baseline lines replaced with var(--space-*), var(--font-size-*), var(--font-weight-*) references"
      contains: "var(--space-"
    - path: "resources/views/livewire/guest-menu.blade.php"
      provides: "Inline `gap:4px` (lines 226, 303) and `margin-right:4px` (lines 171, 229, 306) replaced with var(--space-1) references; sibling non-target literal-value properties (text-decoration, opacity, display, align-items, justify-content) preserved"
      contains: "var(--space-1)"
  key_links:
    - from: "resources/views/livewire/{shop-settings,guest-menu}.blade.php"
      to: "resources/css/app.css :root spacing/typography tokens"
      via: "var(--space-N), var(--font-size-N), var(--font-weight-N) references in inline style attributes"
      pattern: "var\\(--(space|font-size|font-weight)-"
    - from: "resources/views/livewire/{pos-dashboard,super-admin/shops/index}.blade.php"
      to: "resources/css/app.css :root tokens"
      via: "no inline-style sweep needed (baseline 0 matches under two-pass D-26) — verification grep returns zero"
      pattern: "(font-size|padding|margin|gap)\\s*:\\s*[0-9]"
---

<objective>
Add 12 spacing tokens to the existing `:root` block in `resources/css/app.css` (after Plan 10-01's typography tokens), then sweep `resources/views/livewire/guest-menu.blade.php` and `resources/views/livewire/shop-settings.blade.php` to replace every literal-value `font-size:`, `padding:` (including dashed sub-properties), `margin:` (including dashed sub-properties), `gap:` declaration flagged by the **two-pass D-26 grep** with `var(--font-size-*)`, `var(--space-*)`, `var(--font-weight-*)` references. Reduce the baseline of **14 matches** across the four verification screens (5 in guest-menu, 9 in shop-settings, 0 in pos-dashboard and super-admin/shops/index) to **0**.

Purpose: Land the spacing half of the design token system (DS-02) AND apply both halves on the four ROADMAP verification screens so success criteria #2 and #3 ("reduced to zero on those four screens") become verifiable under the two-pass D-26 verification. After this plan, the four screens render typography and spacing exclusively from `:root` tokens.

Output: `resources/css/app.css` extended with 12 spacing tokens; `guest-menu.blade.php` and `shop-settings.blade.php` with literal-value style declarations replaced by token references; the other two verification screens left untouched (already clean under two-pass D-26).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-design-tokens/10-CONTEXT.md
@.planning/phases/10-design-tokens/10-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Spacing scale from CONTEXT D-10 — locked. -->

```css
/* To insert inside resources/css/app.css :root, after Plan 10-01's typography tokens,
   before the closing brace of :root. */
--space-1:  4px;
--space-2:  8px;
--space-3:  12px;
--space-4:  16px;
--space-5:  20px;
--space-6:  24px;
--space-7:  32px;
--space-8:  40px;
--space-9:  48px;
--space-10: 64px;
--space-11: 80px;
--space-12: 96px;
```

<!-- Px-to-token mapping for the sweep. Use this as the substitution table. -->

```
2px   -> closest = 4px = var(--space-1)   /* +2px delta on small margins; visually negligible */
3px   -> closest = 4px = var(--space-1)
4px   -> var(--space-1)        /* exact */
5px   -> closest = 4px = var(--space-1) (under 6) OR 8px = var(--space-2) (over 6) — pick 4 for tight UI, 8 for comfortable */
6px   -> closest = 8px = var(--space-2)   /* +2px delta */
7px   -> closest = 8px = var(--space-2)
8px   -> var(--space-2)        /* exact */
9px   -> closest = 8px = var(--space-2)
10px  -> closest = 12px = var(--space-3)
11px  -> font-size: 11px -> var(--font-size-xs); spacing: 11px -> var(--space-3)
12px  -> var(--space-3) for spacing; var(--font-size-xs) for font-size — context-dependent
14px  -> var(--space-3) for spacing; var(--font-size-sm) for font-size — context-dependent
16px  -> var(--space-4) for spacing; var(--font-size-base) for font-size — context-dependent
18px  -> var(--font-size-md) when font-size; var(--space-5) when spacing
20px  -> var(--space-5)
22px  -> var(--font-size-lg)
24px  -> var(--space-6)
28px  -> var(--font-size-xl)
32px  -> var(--space-7)
34px  -> var(--font-size-2xl)
40px  -> var(--space-8)
48px  -> var(--space-9)
64px  -> var(--space-10)
80px  -> var(--space-11)
96px  -> var(--space-12)

Font-weight literals (per Plan 10-01 tokens):
400 -> var(--font-weight-regular)
500 -> var(--font-weight-medium)
600 -> var(--font-weight-semibold)
700 -> var(--font-weight-bold)
```

<!-- Compound style-attribute strategy: when one inline style contains MULTIPLE properties
     including target ones, rewrite the whole declaration list with token references for
     target properties and PRESERVE all other properties unchanged. Preserve original
     whitespace and quote style of each `style="..."` attribute. -->

<!-- ============================================================================ -->
<!-- BASELINE under the TWO-PASS D-26 literal-value grep (CONTEXT.md, 2026-04-28) -->
<!-- ============================================================================ -->
<!-- Verbatim grep from CONTEXT D-26 (re-baselined by orchestrator 2026-04-28):   -->
<!--                                                                               -->
<!--   PROPS='\b(font-size|padding|padding-(top|right|bottom|left)|margin|margin-(top|right|bottom|left)|gap|row-gap|column-gap)\s*:\s*[0-9]+(\.[0-9]+)?(px|rem|em|%)' -->
<!--                                                                               -->
<!--   grep -En 'style="[^"]*"' \                                                  -->
<!--     resources/views/livewire/guest-menu.blade.php \                           -->
<!--     resources/views/livewire/pos-dashboard.blade.php \                        -->
<!--     resources/views/livewire/shop-settings.blade.php \                        -->
<!--     resources/views/livewire/super-admin/shops/index.blade.php \              -->
<!--     | grep -E "$PROPS"                                                        -->
<!--                                                                               -->
<!-- Per-screen baseline (verified 2026-04-28):                                   -->
<!--                                                                               -->
<!--   guest-menu.blade.php:               5 matches                              -->
<!--     Line 171: <span style="text-decoration:line-through;opacity:0.5;margin-right:4px"> -->
<!--     Line 226: <div  style="display:flex;align-items:center;justify-content:space-between;gap:4px"> -->
<!--     Line 229: <span style="text-decoration:line-through;opacity:0.5;margin-right:4px"> -->
<!--     Line 303: <div  style="display:flex;align-items:center;justify-content:space-between;gap:4px"> -->
<!--     Line 306: <span style="text-decoration:line-through;opacity:0.5;margin-right:4px"> -->
<!--                                                                               -->
<!--   shop-settings.blade.php:            9 matches                              -->
<!--     Line 25:  <p ... style="margin-bottom: 12px;">Menu Theme</p>             -->
<!--     Line 28:  <div   style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px;"> -->
<!--     Line 47:  <span  style="position: absolute; top: 6px; right: 6px; width: 20px; height: 20px; border-radius: 50%; background: rgb(var(--crema)); color: #fff; display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: 700; z-index: 2; box-shadow: 0 1px 4px rgb(0 0 0 / 0.25);"> -->
<!--     Line 50:  <div   style="background: {{ $t['bg'] }}; padding: 10px; aspect-ratio: 4 / 3; display: flex; flex-direction: column; gap: 4px; justify-content: center; overflow: hidden;"> -->
<!--     Line 54:  <div   style="padding: 3px 5px;">                              -->
<!--     Line 62:  <div   style="padding: 7px 6px 9px; border-top: 1px solid rgb(var(--line) / 0.4); font-size: 12px; font-weight: 600; color: rgb(var(--ink));"> -->
<!--     Line 70:  <div   style="border: 1px solid rgb(var(--line)); border-radius: 10px; overflow: hidden; margin-top: 14px;"> -->
<!--     Line 76:  <div   style="padding: 14px;">                                 -->
<!--     Line 102: <div   style="padding: 8px;">                                  -->
<!--                                                                               -->
<!--   pos-dashboard.blade.php:            0 matches                              -->
<!--   super-admin/shops/index.blade.php:  0 matches                              -->
<!--                                                                               -->
<!--   TOTAL BASELINE = 14 matches; sweep must drive to 0.                        -->
<!-- ============================================================================ -->

<!-- ============================================================================ -->
<!-- Line 43 in shop-settings.blade.php — DO NOT TOUCH                            -->
<!-- ============================================================================ -->
<!-- The button on line 43 has inline style: `padding: 0; border-radius: 10px; …`. -->
<!-- The two-pass D-26 grep correctly EXCLUDES this line:                         -->
<!--   • `padding: 0` has no unit (the prop:literal regex requires px|rem|em|%),  -->
<!--   • `border-radius` is NOT in the prop list,                                 -->
<!--   so neither value triggers the verification.                                -->
<!--                                                                               -->
<!-- Iteration 1's revision incorrectly removed `padding: 0;` from this line      -->
<!-- because the iteration-1 grep over-matched on the sibling `border-radius:     -->
<!-- 10px`. The two-pass grep makes that fix unnecessary. Per the iteration-2     -->
<!-- revision context: "Restore `padding: 0;` to the line 43 inline style (or     -->
<!-- leave it untouched if iteration 1's edit hasn't been applied to source yet — -->
<!-- the plan executes later via /gsd-execute-phase, not now)."                   -->
<!--                                                                               -->
<!-- → Task 2 substeps DO NOT include line 43.                                    -->
<!-- → If executor finds line 43 already missing `padding: 0;` (some prior        -->
<!--   tooling), restore it; otherwise leave it byte-identical to its source-tree -->
<!--   state.                                                                      -->
<!-- ============================================================================ -->

<!-- Specific samples to inform the executor about compound styles: -->

```html
<!-- shop-settings.blade.php line 28: -->
<div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px;">
<!-- 10px is not a token; round to 12px -> var(--space-3); +2px on a 3-col grid is
     visually trivial. Substitute. -->
<div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-3);">

<!-- shop-settings.blade.php line 47 (compound, includes font-size + font-weight): -->
<span style="position: absolute; top: 6px; right: 6px; width: 20px; height: 20px;
       border-radius: 50%; background: rgb(var(--crema)); color: #fff; display: flex;
       align-items: center; justify-content: center; font-size: 11px; font-weight: 700;
       z-index: 2; box-shadow: 0 1px 4px rgb(0 0 0 / 0.25);">
<!-- top:6px, right:6px, width:20px, height:20px are NOT in the four target properties
     (font-size|padding|margin|gap|sub-properties), so they stay literal.
     font-size: 11px -> font-size: var(--font-size-xs) (closest token = 12px).
     font-weight: 700 -> font-weight: var(--font-weight-bold) (Plan 10-01 token). -->

<!-- guest-menu.blade.php line 226 (gap inside compound style): -->
<div style="display:flex;align-items:center;justify-content:space-between;gap:4px">
<!-- gap:4px is a packed value (no whitespace after `:`). The two-pass D-26 grep
     catches it. Substitute:                                                  -->
<div style="display:flex;align-items:center;justify-content:space-between;gap:var(--space-1)">

<!-- guest-menu.blade.php line 171 (margin-right inside compound style): -->
<span style="text-decoration:line-through;opacity:0.5;margin-right:4px">
<!-- margin-right is a dashed sub-property; the two-pass grep prop list includes it.
     Other properties (text-decoration, opacity) are NOT in the target list — preserve. -->
<span style="text-decoration:line-through;opacity:0.5;margin-right:var(--space-1)">
```

<!-- INLINE STYLES TO LEAVE ALONE (NOT flagged by two-pass D-26): -->

```html
<!-- guest-menu.blade.php — every other inline style in the file (color, opacity, position,
     transition, dynamic Blade interpolations) is NOT in the prop list and stays. -->

<!-- guest-menu.blade.php line 50, 404 — DYNAMIC color, not a target property: PRESERVE -->
<span style="background-color: {{ $participantColors[$participantId] ?? '#E57373' }}">

<!-- guest-menu.blade.php image fade — opacity/transition not in target prop list: PRESERVE -->
<img style="opacity: 0; transition: opacity 200ms ease">

<!-- guest-menu.blade.php skeleton loader — position/inset/border-radius not in prop list: PRESERVE -->
<div style="position:absolute;inset:0;border-radius:0">

<!-- guest-menu.blade.php product-name color: not a target property: PRESERVE -->
<p style="color: rgb(var(--ink));">

<!-- shop-settings.blade.php line 43 — `padding: 0` (no unit) and `border-radius: 10px`
     (not in prop list): NOT flagged; PRESERVE the entire inline style as-is. -->
<button … style="position: relative; display: block; padding: 0; border-radius: 10px; cursor: pointer; …">

<!-- shop-settings.blade.php lines 103, 108 — Alpine `:style="..."` template literals.
     The two-pass D-26 grep matches `style="..."` (no leading colon), not `:style="..."`,
     so these are NOT in scope under D-26. Phase 14 will pick them up via a separate sweep. -->
<div :style="`font-size: 11px; font-weight: 600; transition: color 300ms ease; color: rgb(${ … });`">
```

<!-- D-19 BOUNDARY: Shared partials (header, layout) NOT swept unless they directly cause a
     verification grep on a target screen to fail. This plan only edits guest-menu and
     shop-settings; pos-dashboard and super-admin/shops/index are listed in files_modified
     for traceability/file-ownership only (no edits — already 0 matches). -->

<!-- Verification grep (D-26 — exact two-pass form from CONTEXT.md, 2026-04-28) -->
```bash
PROPS='\b(font-size|padding|padding-(top|right|bottom|left)|margin|margin-(top|right|bottom|left)|gap|row-gap|column-gap)\s*:\s*[0-9]+(\.[0-9]+)?(px|rem|em|%)'

grep -En 'style="[^"]*"' \
  resources/views/livewire/guest-menu.blade.php \
  resources/views/livewire/pos-dashboard.blade.php \
  resources/views/livewire/shop-settings.blade.php \
  resources/views/livewire/super-admin/shops/index.blade.php \
  | grep -E "$PROPS"
# Expected output: zero lines.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert spacing tokens into :root (and regression-check Plan 10-01 typography tokens)</name>
  <files>resources/css/app.css</files>
  <read_first>
    - resources/css/app.css :root block (post-Plan-10-01 state, including the typography tokens just added)
    - .planning/phases/10-design-tokens/10-CONTEXT.md §Decisions D-10, D-11, D-12 (spacing values, rationale, location)
    - .planning/phases/10-design-tokens/10-01-SUMMARY.md (verify Plan 10-01 landed and identify the exact line where typography tokens end)
  </read_first>
  <action>
    **Step 1 — Locate the insertion point.**

    Open `resources/css/app.css`. After Plan 10-01, the `:root` block now contains color tokens, typography sizes, font weights, and Latin line-heights. Find the last line-height token (`--line-height-loose: 1.70;`) inside `:root`. Insert the spacing tokens immediately AFTER that line and BEFORE the closing `}` of `:root`.

    **Step 2 — Insert the spacing token block.**

    Add (preserving 8-space indentation matching existing tokens):

    ```css

            /* Spacing scale — 4px base, mixed progression (D-10).
               Fine-grained 4-24px for tight UI; geometric 32-96px for layout sections.
               No --space-0 (use property omission); no values >96px (use composition). */
            --space-1:  4px;
            --space-2:  8px;
            --space-3:  12px;
            --space-4:  16px;
            --space-5:  20px;
            --space-6:  24px;
            --space-7:  32px;
            --space-8:  40px;
            --space-9:  48px;
            --space-10: 64px;
            --space-11: 80px;
            --space-12: 96px;
    ```

    **Step 3 — Do NOT touch anything else.**

    Color tokens, typography tokens (Plan 10-01), `[lang="ar"]` override, html/body, `@layer components`, `[data-theme="..."]` blocks all stay byte-identical.

    **Step 4 — Build assets.**

    `npm run build` to confirm Vite compiles cleanly.
  </action>
  <verify>
    <automated>grep -cE '\-\-space-([1-9]|1[0-2]):' resources/css/app.css | grep -q '^12$' && grep -cE '\-\-font-size-(xs|sm|base|md|lg|xl|2xl)' resources/css/app.css | grep -q '^7$' && grep -cE '\-\-font-weight-(regular|medium|semibold|bold)' resources/css/app.css | grep -q '^4$' && grep -c '\-\-line-height-' resources/css/app.css | grep -q '^6$' && grep -A4 '\[lang="ar"\]' resources/css/app.css | grep -q '1\.35' && grep -A4 '\[lang="ar"\]' resources/css/app.css | grep -q '1\.65' && grep -A4 '\[lang="ar"\]' resources/css/app.css | grep -q '1\.85' && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\-\-space-([1-9]|1[0-2]):' resources/css/app.css` returns exactly 12
    - `grep -nE '\-\-space-1:\s*4px' resources/css/app.css` returns at least one match
    - `grep -nE '\-\-space-12:\s*96px' resources/css/app.css` returns at least one match
    - **Regression-check Plan 10-01:** `grep -cE '\-\-font-size-(xs|sm|base|md|lg|xl|2xl)' resources/css/app.css` still returns 7
    - **Regression-check Plan 10-01:** `grep -cE '\-\-font-weight-(regular|medium|semibold|bold)' resources/css/app.css` still returns 4
    - **Regression-check Plan 10-01:** `grep -c '\-\-line-height-' resources/css/app.css` still returns 6 (3 Latin + 3 Arabic)
    - **Regression-check Plan 10-01:** `grep -A4 '\[lang="ar"\]' resources/css/app.css` still contains literals `1.35`, `1.65`, AND `1.85`
    - `grep -c '@apply' resources/css/app.css` returns 14 (component layer untouched)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>12 spacing tokens added inside the existing `:root` block, after the typography tokens, with the 4-step section comment. Vite builds cleanly. All Plan 10-01 typography tokens (sizes, weights, both line-height tracks) are still present and unmodified. Component layer untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep guest-menu.blade.php and shop-settings.blade.php — drive two-pass D-26 grep across the four screens to zero (14 → 0)</name>
  <files>resources/views/livewire/guest-menu.blade.php, resources/views/livewire/shop-settings.blade.php</files>
  <read_first>
    - resources/views/livewire/guest-menu.blade.php lines 165-310 (5 matches under two-pass D-26 on lines 171, 226, 229, 303, 306 — newly in scope this iteration)
    - resources/views/livewire/shop-settings.blade.php lines 20-115 (9 matches under two-pass D-26 on lines 25, 28, 47, 50, 54, 62, 70, 76, 102 — densest sweep target)
    - resources/views/livewire/pos-dashboard.blade.php (full file — confirm 0 matches under two-pass D-26; nothing to change)
    - resources/views/livewire/super-admin/shops/index.blade.php (full file — 58 lines; confirm 0 matches; nothing to change)
    - .planning/phases/10-design-tokens/10-CONTEXT.md §D-17, D-18, D-19 (sweep boundary; what to leave alone)
    - .planning/phases/10-design-tokens/10-CONTEXT.md §D-26 (the EXACT two-pass verification grep — the contract this task drives to zero)
    - <interfaces> block above for the px-to-token mapping table, the line 43 do-not-touch note, and the per-line compound-style examples
  </read_first>
  <action>
    **Sweep targets:** `resources/views/livewire/guest-menu.blade.php` (5 matches) and `resources/views/livewire/shop-settings.blade.php` (9 matches). The other two files in `files_modified` are listed for traceability — they have 0 baseline matches under the two-pass D-26 grep and require no edits.

    **General rules:**
    1. Only modify the 14 lines flagged by the two-pass D-26 grep:
       - guest-menu: lines 171, 226, 229, 303, 306
       - shop-settings: lines 25, 28, 47, 50, 54, 62, 70, 76, 102
       Leave every other line in both files alone.
    2. **DO NOT touch line 43 of shop-settings.blade.php.** The two-pass D-26 grep correctly excludes it (`padding: 0` has no unit; `border-radius: 10px` is not in the prop list). Iteration 1's revision incorrectly removed `padding: 0;` from this line — that change is REVERTED by this iteration. If the source file currently has line 43 with `padding: 0;` present, leave it. If iteration 1's edit was already applied to source (it shouldn't have been — plans execute later), restore `padding: 0;` to the original `style="position: relative; display: block; padding: 0; border-radius: 10px; cursor: pointer; background: rgb(var(--panel)); text-align: center; width: 100%; overflow: hidden; transition: border-color 200ms ease, box-shadow 200ms ease;"`.
    3. Within each flagged line, only modify properties matching the two-pass D-26 prop list: `font-size`, `padding`, `padding-(top|right|bottom|left)`, `margin`, `margin-(top|right|bottom|left)`, `gap`, `row-gap`, `column-gap`. Tokenize `font-weight` opportunistically when present on the same line (Plan 10-01 added the weight tokens). Leave every other CSS property in the inline style alone.
    4. NEVER change a `style="..."` attribute that contains a Blade `{{ ... }}` interpolation in a target property (none currently exist in the flagged lines, but the rule prevents future dynamic-value drift).
    5. **DO NOT touch Alpine `:style="..."` template literals on shop-settings lines 103, 108.** The two-pass D-26 grep matches `style="..."`, not `:style="..."`, so these are out of D-26 scope. They are queued for Phase 14.
    6. Preserve original whitespace and quote style of each `style="..."` attribute (some use `style="a:b; c:d;"` with spaces, some use `style="a:b;c:d"` without — match what's there).
    7. Re-run the two-pass D-26 grep after each edit to track progress toward 0.

    ---

    **Per-line edits — guest-menu.blade.php (5 substitutions):**

    1. **Line 171:**
       - **Before:** `<span style="text-decoration:line-through;opacity:0.5;margin-right:4px"><x-price :amount="$product->price" :shop="$shop" /></span>`
       - **After:** `<span style="text-decoration:line-through;opacity:0.5;margin-right:var(--space-1)"><x-price :amount="$product->price" :shop="$shop" /></span>`
       - Substitution: `margin-right:4px` → `margin-right:var(--space-1)` (exact 4px → space-1 match). Sibling `text-decoration:line-through;opacity:0.5;` preserved (not in prop list).

    2. **Line 226:**
       - **Before:** `<div style="display:flex;align-items:center;justify-content:space-between;gap:4px">`
       - **After:** `<div style="display:flex;align-items:center;justify-content:space-between;gap:var(--space-1)">`
       - Substitution: `gap:4px` → `gap:var(--space-1)` (exact). Sibling `display:flex;align-items:center;justify-content:space-between;` preserved.

    3. **Line 229:**
       - **Before:** `<span style="text-decoration:line-through;opacity:0.5;margin-right:4px"><x-price :amount="$product->price" :shop="$shop" /></span>`
       - **After:** `<span style="text-decoration:line-through;opacity:0.5;margin-right:var(--space-1)"><x-price :amount="$product->price" :shop="$shop" /></span>`
       - Same substitution as line 171 (this is the dark-overlay menu card variant).

    4. **Line 303:**
       - **Before:** `<div style="display:flex;align-items:center;justify-content:space-between;gap:4px">`
       - **After:** `<div style="display:flex;align-items:center;justify-content:space-between;gap:var(--space-1)">`
       - Same substitution as line 226 (this is the third menu card variant — quick-add row).

    5. **Line 306:**
       - **Before:** `<span style="text-decoration:line-through;opacity:0.5;margin-right:4px"><x-price :amount="$product->price" :shop="$shop" /></span>`
       - **After:** `<span style="text-decoration:line-through;opacity:0.5;margin-right:var(--space-1)"><x-price :amount="$product->price" :shop="$shop" /></span>`
       - Same substitution as lines 171/229 (third menu card variant).

    All 5 guest-menu substitutions are exact 4px → `var(--space-1)` matches with **zero visual delta**. Sibling non-target properties (`text-decoration`, `opacity`, `display`, `align-items`, `justify-content`) are byte-identical pre/post.

    ---

    **Per-line edits — shop-settings.blade.php (9 substitutions):**

    1. **Line 25:** `<p class="font-mono text-[10px] font-semibold uppercase tracking-[0.16em] text-ink-soft" style="margin-bottom: 12px;">Menu Theme</p>`
       - Substitution: `margin-bottom: 12px;` → `margin-bottom: var(--space-3);` (exact 12px → space-3 match)
       - Result: `<p class="font-mono text-[10px] font-semibold uppercase tracking-[0.16em] text-ink-soft" style="margin-bottom: var(--space-3);">Menu Theme</p>`

    2. **Line 28:** `<div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px;">`
       - Substitution: `gap: 10px;` → `gap: var(--space-3);` (10 → 12 = +2px on a 3-col grid; visually negligible)
       - Result: `<div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-3);">`

    3. **Line 47** (compound — checkmark badge):
       - Substitution 1: `font-size: 11px;` → `font-size: var(--font-size-xs);` (11 → 12 = +1px)
       - Substitution 2: `font-weight: 700;` → `font-weight: var(--font-weight-bold);` (exact)
       - Other properties (`position`, `top`, `right`, `width`, `height`, `border-radius`, `background`, `color`, `display`, `align-items`, `justify-content`, `z-index`, `box-shadow`) stay literal — out of Phase 10's sweep scope.

    4. **Line 50** (compound — theme-tile mockup): `<div style="background: {{ $t['bg'] }}; padding: 10px; aspect-ratio: 4 / 3; display: flex; flex-direction: column; gap: 4px; justify-content: center; overflow: hidden;">`
       - Substitution 1: `padding: 10px;` → `padding: var(--space-3);` (10 → 12 = +2px each side; acceptable)
       - Substitution 2: `gap: 4px;` → `gap: var(--space-1);` (exact)
       - Other properties preserved (`background: {{ $t['bg'] }}` is a Blade interpolation — preserve untouched; `aspect-ratio`, `display`, `flex-direction`, `justify-content`, `overflow` are not in target list).

    5. **Line 54:** `<div style="padding: 3px 5px;">`
       - Substitution: `padding: 3px 5px;` → `padding: var(--space-1) var(--space-1);` (3 → 4 = +1px vertical; 5 → 4 = -1px horizontal; visually negligible). Equivalent shorter form: `padding: var(--space-1);`. Use the explicit two-value form to preserve the original author's intent that the inner padding is asymmetric (even if both values now collapse to the same token).

    6. **Line 62** (compound — label row): `<div style="padding: 7px 6px 9px; border-top: 1px solid rgb(var(--line) / 0.4); font-size: 12px; font-weight: 600; color: rgb(var(--ink));">`
       - Substitution 1: `padding: 7px 6px 9px;` → `padding: var(--space-2) var(--space-2) var(--space-2);` (7 → 8 = +1px top; 6 → 8 = +2px L/R; 9 → 8 = -1px bottom). Equivalent shorter form: `padding: var(--space-2);`. Use the explicit three-value form to preserve the author's intent.
       - Substitution 2: `font-size: 12px;` → `font-size: var(--font-size-xs);` (exact 12px match)
       - Substitution 3: `font-weight: 600;` → `font-weight: var(--font-weight-semibold);` (exact)
       - `border-top` and `color` preserved.

    7. **Line 70:** `<div style="border: 1px solid rgb(var(--line)); border-radius: 10px; overflow: hidden; margin-top: 14px;">`
       - Substitution: `margin-top: 14px;` → `margin-top: var(--space-3);` (14 → 12 = -2px; acceptable)
       - Other properties (`border`, `border-radius`, `overflow`) preserved.

    8. **Line 76:** `<div style="padding: 14px;">`
       - Substitution: `padding: 14px;` → `padding: var(--space-3);` (14 → 12 = -2px; acceptable)

    9. **Line 102:** `<div style="padding: 8px;">`
       - Substitution: `padding: 8px;` → `padding: var(--space-2);` (exact)

    ---

    **After each edit, run the two-pass D-26 grep on the file just edited:**

    ```bash
    PROPS='\b(font-size|padding|padding-(top|right|bottom|left)|margin|margin-(top|right|bottom|left)|gap|row-gap|column-gap)\s*:\s*[0-9]+(\.[0-9]+)?(px|rem|em|%)'
    grep -En 'style="[^"]*"' resources/views/livewire/<file> | grep -E "$PROPS"
    ```

    Expected after all 14 edits: zero output for both files individually, and zero output for the master cross-screen grep below.

    **Final cross-screen verification:**

    Run the two-pass D-26 master grep across all four files:
    ```bash
    PROPS='\b(font-size|padding|padding-(top|right|bottom|left)|margin|margin-(top|right|bottom|left)|gap|row-gap|column-gap)\s*:\s*[0-9]+(\.[0-9]+)?(px|rem|em|%)'

    grep -En 'style="[^"]*"' \
      resources/views/livewire/guest-menu.blade.php \
      resources/views/livewire/pos-dashboard.blade.php \
      resources/views/livewire/shop-settings.blade.php \
      resources/views/livewire/super-admin/shops/index.blade.php \
      | grep -E "$PROPS"
    ```
    Expected: zero output across all four files. Baseline was 14 (5 in guest-menu + 9 in shop-settings). Delta = -14.

    Run lint and build:
    - `./vendor/bin/pint --test` — should be a no-op (no PHP changed)
    - `npm run build` — confirms Vite still compiles app.css and the views with token references

    Run `composer test` — the Livewire feature tests for guest menu, POS, and shop settings should still pass. The substitutions are pure CSS and should not affect Livewire state, prop validation, or backend behavior.
  </action>
  <verify>
    <automated>PROPS='\b(font-size|padding|padding-(top|right|bottom|left)|margin|margin-(top|right|bottom|left)|gap|row-gap|column-gap)\s*:\s*[0-9]+(\.[0-9]+)?(px|rem|em|%)'; COUNT=$(grep -En 'style="[^"]*"' resources/views/livewire/guest-menu.blade.php resources/views/livewire/pos-dashboard.blade.php resources/views/livewire/shop-settings.blade.php resources/views/livewire/super-admin/shops/index.blade.php | grep -Ec "$PROPS"); test "$COUNT" = "0" && composer test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - **Two-pass D-26 master grep returns ZERO lines** across all four verification screens (down from baseline 14):
      ```
      PROPS='\b(font-size|padding|padding-(top|right|bottom|left)|margin|margin-(top|right|bottom|left)|gap|row-gap|column-gap)\s*:\s*[0-9]+(\.[0-9]+)?(px|rem|em|%)'

      grep -En 'style="[^"]*"' \
        resources/views/livewire/guest-menu.blade.php \
        resources/views/livewire/pos-dashboard.blade.php \
        resources/views/livewire/shop-settings.blade.php \
        resources/views/livewire/super-admin/shops/index.blade.php \
        | grep -E "$PROPS"
      ```
    - `grep -c 'var(--space-1)' resources/views/livewire/guest-menu.blade.php` returns at least 5 (lines 171, 226, 229, 303, 306 each got one space-1 substitution)
    - `grep -c 'var(--space-' resources/views/livewire/shop-settings.blade.php` returns at least 8 (lines 25, 28, 50 (×2), 54, 62, 70, 76, 102 — at least 8 distinct space-* tokens introduced; line 102 = space-2; lines 25/28/50/70/76 = space-3; line 50/54/62 use space-1 or space-2)
    - `grep -c 'var(--font-size-' resources/views/livewire/shop-settings.blade.php` returns at least 2 (lines 47, 62 have font-size substitutions; lines 103/108 are NOT touched because they use `:style="..."`)
    - `grep -c 'var(--font-weight-' resources/views/livewire/shop-settings.blade.php` returns at least 2 (lines 47, 62 have font-weight substitutions)
    - **Line 43 of shop-settings.blade.php is byte-identical to source-tree state** — `padding: 0;` is present in the inline style. Verify: `grep -nE 'style="[^"]*padding:\s*0;[^"]*border-radius:\s*10px' resources/views/livewire/shop-settings.blade.php` returns at least one match (line 43).
    - **Lines 103 and 108 of shop-settings.blade.php (Alpine `:style` template literals) are byte-identical to source-tree state** — `font-size: 11px`, `font-size: 9px`, `font-weight: 600`, `margin-top: 2px` are all preserved. Verify: `grep -c 'font-size: 11px\|font-size: 9px\|margin-top: 2px' resources/views/livewire/shop-settings.blade.php` returns at least 3.
    - Inline styles encoding DYNAMIC values (e.g., `style="background-color: {{ $participantColors[...] }}"` on guest-menu lines 50, 404; `background: {{ $t['bg'] }}` on shop-settings line 50) are PRESERVED unchanged — `grep -c 'participantColors' resources/views/livewire/guest-menu.blade.php` matches its pre-plan count (2); `grep -c "{{ \$t\['bg'\] }}" resources/views/livewire/shop-settings.blade.php` matches its pre-plan count.
    - `composer test` exits 0 with no test regressions
    - `./vendor/bin/pint --test` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>guest-menu.blade.php and shop-settings.blade.php contain zero lines flagged by the two-pass D-26 grep. The other two verification screens (pos-dashboard, super-admin/shops/index) are untouched (already 0 under two-pass D-26). Line 43 of shop-settings retains its original `padding: 0;`. Lines 103/108 (Alpine `:style`) retain their literal CSS values. All non-target inline-style properties (color, background, position, opacity, transition, dynamic Blade values, Alpine `${…}` interpolations) are byte-identical to pre-plan state. Tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify the four verification screens render correctly post-sweep</name>
  <what-built>
    - 12 spacing tokens (`--space-1` through `--space-12`) added to `:root` in `app.css`
    - 5 inline-style flags on guest-menu.blade.php (lines 171, 226, 229, 303, 306) resolved by tokenizing `margin-right:4px` → `margin-right:var(--space-1)` and `gap:4px` → `gap:var(--space-1)`. Zero visual delta — all five are exact 4px → `var(--space-1)` matches.
    - 9 inline-style flags on shop-settings.blade.php (lines 25, 28, 47, 50, 54, 62, 70, 76, 102) resolved by tokenizing literal values. Visual deltas: line 25 exact, line 28 +2px gap, line 47 +1px font-size, line 50 +2px padding & exact gap, line 54 -1px/+1px asymmetric padding, line 62 +1px/+2px/-1px asymmetric padding & exact font-size, line 70 -2px margin, line 76 -2px padding, line 102 exact. None exceed ±2px.
    - **Line 43 of shop-settings (theme-tile button) UNCHANGED** — `padding: 0; border-radius: 10px; ...` retained verbatim. The two-pass D-26 grep correctly excludes it.
    - **Lines 103/108 of shop-settings (Alpine `:style` template literals) UNCHANGED** — `font-size: 11px`, `font-size: 9px`, `margin-top: 2px`, `font-weight: 600` retained. Out of D-26 scope (grep matches `style="..."`, not `:style="..."`); queued for Phase 14.
    - pos-dashboard, super-admin/shops/index untouched (already clean under two-pass D-26)
    - Two-pass D-26 grep returns 0 across all four screens (baseline 14 → 0)
  </what-built>
  <how-to-verify>
    1. Run `composer dev` to bring the app up.

    2. **Guest menu (EN, 360px):** Visit `http://localhost:8000/menu/sourdough` in DevTools mobile mode at 360x800. Confirm:
       - The five swept inline-style sites (lines 171, 226, 229, 303, 306) render IDENTICALLY to their pre-plan state — all five substitutions are exact 4px → `var(--space-1)` token references with zero visual delta.
       - Strikethrough sale prices (lines 171, 229, 306) sit 4px to the left of the live price, with line-through and 50% opacity preserved.
       - Quick-add rows (lines 226, 303) maintain a 4px gap between price and the `+` button.
       - DevTools Computed style on any of these elements should now show the resolved value (4px) and the source as `var(--space-1)`.
       - No layout shift, no overlap, no clipped pricing.

    3. **Guest menu (AR, 360px):** Toggle locale to Arabic. Reload `/menu/sourdough`.
       - Same as EN — visual state byte-identical because all substitutions are exact.
       - Confirm Plan 10-01's `[lang="ar"]` line-height override is still active (DevTools Computed: `--line-height-normal` = `1.65`).

    4. **POS dashboard:** Log in as `admin@bite.com` / `password`, visit `/dashboard`. Click around the menu, add items to an order, navigate categories.
       - This screen had 0 baseline matches and 0 substitutions, so visual state should be byte-identical to pre-plan.
       - Confirm no console errors, no broken Livewire interactions.

    5. **Admin shop settings:** Visit `/admin/settings` (Laravel route name `admin.settings`).
       - The "Menu Theme" picker (the 3-up grid of theme tiles) is the densest sweep target. Visually confirm:
         - "Menu Theme" label (line 25) sits 12px above the tile grid (was 12px; exact — no delta).
         - Tiles still align in a 3-column grid with 12px gap (was 10px; +2px difference, very subtle).
         - **Line 43 button (UNCHANGED):** Each theme tile retains its original `padding: 0; border-radius: 10px; ...`. The button hugs its inner mockup as it did pre-plan. **No visual change expected on this element** — if anything looks different from the pre-plan baseline here, an unintended edit was made.
         - Each tile's preview mockup (line 50: was padding 10px, now 12px = +2px each side) has slightly more breathing room around the inner content; gap 4px is exact.
         - The check badge in the top-right of the active tile (line 47) is still 20x20 with a 12px font-size (was 11px; +1px) and bold weight.
         - The 3px-5px inner padding (line 54) now reads as 4px-4px — a 1px squeeze on each axis; if visually material, the asymmetry is preserved by using `var(--space-1) var(--space-1)` form.
         - The 7-6-9 padding (line 62) now reads as 8-8-8 — a 2px squeeze on the horizontal edges; if visually material, flag it for a uniform `var(--space-2)` shorthand.
         - **Theme-tile preview text (lines 103, 108) UNCHANGED:** the item-name preview is still 11px and the item-price preview is still 9px. **No visual change expected** — the Alpine `:style="..."` template literals are not touched in Phase 10. They will be addressed in Phase 14.
       - The Shop Hours toggle / opening hours panel:
         - Line 70 `margin-top: 14px` → 12px (-2px; visually negligible).
         - Line 76 `padding: 14px` → 12px (-2px).
         - Line 102 `padding: 8px` → exact (no delta).

    6. **Super-admin shop list:** Log in as `super@bite.com` / `password`, visit `/super-admin/shops`.
       - 0 substitutions on this screen; visual state byte-identical.

    7. **Run the two-pass D-26 verification grep yourself in the terminal:**
       ```bash
       PROPS='\b(font-size|padding|padding-(top|right|bottom|left)|margin|margin-(top|right|bottom|left)|gap|row-gap|column-gap)\s*:\s*[0-9]+(\.[0-9]+)?(px|rem|em|%)'

       grep -En 'style="[^"]*"' \
         resources/views/livewire/guest-menu.blade.php \
         resources/views/livewire/pos-dashboard.blade.php \
         resources/views/livewire/shop-settings.blade.php \
         resources/views/livewire/super-admin/shops/index.blade.php \
         | grep -E "$PROPS"
       ```
       Expected: zero lines. Baseline was 14 (5 guest-menu + 9 shop-settings).

    8. **Run the test suite yourself:** `composer test` — should be green.
  </how-to-verify>
  <resume-signal>Type "approved" if the four screens render correctly with no functional regression and the px-rounding tweaks on shop-settings are acceptable. The highest-risk areas are: (a) shop-settings line 50 (theme-tile mockup padding +2px each side); (b) shop-settings line 62 (asymmetric padding squeeze 7-6-9 → 8-8-8 with horizontal -2px); (c) verifying line 43 (theme-tile button) is BYTE-IDENTICAL to pre-plan (i.e., `padding: 0;` is still there); (d) verifying lines 103/108 (Alpine `:style` template literals) are BYTE-IDENTICAL to pre-plan. If any are unacceptable, describe the issue and the executor will adjust.</resume-signal>
</task>

</tasks>

<verification>
1. `grep -cE '\-\-space-([1-9]|1[0-2]):' resources/css/app.css` returns exactly 12
2. **Two-pass D-26 grep** across the four screens returns ZERO lines:
   ```
   PROPS='\b(font-size|padding|padding-(top|right|bottom|left)|margin|margin-(top|right|bottom|left)|gap|row-gap|column-gap)\s*:\s*[0-9]+(\.[0-9]+)?(px|rem|em|%)'

   grep -En 'style="[^"]*"' \
     resources/views/livewire/guest-menu.blade.php \
     resources/views/livewire/pos-dashboard.blade.php \
     resources/views/livewire/shop-settings.blade.php \
     resources/views/livewire/super-admin/shops/index.blade.php \
     | grep -E "$PROPS"
   ```
3. `grep -c 'var(--space-1)' resources/views/livewire/guest-menu.blade.php` returns ≥ 5
4. `grep -c 'var(--space-' resources/views/livewire/shop-settings.blade.php` returns ≥ 8
5. `grep -c 'var(--font-size-' resources/views/livewire/shop-settings.blade.php` returns ≥ 2
6. `grep -c 'var(--font-weight-' resources/views/livewire/shop-settings.blade.php` returns ≥ 2
7. **Line 43 byte-identical:** `grep -nE 'padding:\s*0;[^"]*border-radius:\s*10px' resources/views/livewire/shop-settings.blade.php` returns line 43
8. **Lines 103/108 byte-identical:** `grep -c 'font-size: 11px\|font-size: 9px\|margin-top: 2px' resources/views/livewire/shop-settings.blade.php` returns ≥ 3
9. **Plan 10-01 regression checks pass:** 7 font-size tokens, 4 font-weight tokens, 6 line-height tokens, AR override values 1.35/1.65/1.85 still present in app.css
10. `grep -c '@apply' resources/css/app.css` still 14 (component layer untouched)
11. `composer test` exits 0
12. `./vendor/bin/pint --test` exits 0
13. `npm run build` exits 0
14. Manual checkpoint passes: 4 verification screens render correctly in EN and AR (where applicable) at 360px viewport with no functional regression.
</verification>

<success_criteria>
- 12 spacing tokens defined inside the existing `:root` block, after Plan 10-01's typography tokens (covers DS-02 token-definition half)
- All 14 baseline matches of the two-pass D-26 grep across the four verification screens (5 in guest-menu + 9 in shop-settings) reduced to 0 (covers DS-01/DS-02 token-application half on the four screens; ROADMAP success criteria #2 and #3 satisfied under the two-pass D-26 contract)
- Plan 10-01's typography tokens (7 sizes + 4 weights + 6 line-heights including AR overrides 1.35/1.65/1.85) regression-checked and present
- All non-target inline-style properties preserved unchanged (color, position, opacity, transition, dynamic Blade interpolations, Alpine `${…}` interpolations)
- Line 43 of shop-settings.blade.php (`padding: 0; border-radius: 10px; ...`) is BYTE-IDENTICAL to source-tree state — the two-pass D-26 grep correctly excludes it
- Lines 103/108 of shop-settings.blade.php (Alpine `:style` template literals) are BYTE-IDENTICAL to source-tree state — out of D-26 scope; queued for Phase 14
- @apply component layer in `app.css` untouched (D-14/D-15 honored)
- Other screens (KDS, billing, super-admin user management, etc.) untouched (D-18 boundary respected)
- Shared partials untouched (D-19 boundary respected)
- Test suite green; Vite builds; Pint passes
- Manual visual check confirms no regression in EN or AR at 360px viewport
</success_criteria>

<output>
After completion, create `.planning/phases/10-design-tokens/10-02-SUMMARY.md` documenting:
(a) before/after match counts under the two-pass D-26 grep per file (baseline: 5 in guest-menu, 9 in shop-settings, 0 in pos-dashboard, 0 in super-admin/shops/index — total 14; final: 0 across all four)
(b) px-rounding decisions and the rationale (largest deltas: line 28 gap 10→12 = +2px; line 50 padding 10→12 = +2px each side; line 62 padding asymmetry 7-6-9 → 8-8-8 = +1/+2/-1px). None exceed ±2px.
(c) confirmation that line 43 of shop-settings was NOT modified (the iteration-1 over-match fix was reverted; two-pass D-26 grep correctly excludes it)
(d) confirmation that lines 103/108 (Alpine `:style` template literals) were NOT modified — out of D-26 scope; queued for Phase 14
(e) confirmation that pos-dashboard.blade.php and super-admin/shops/index.blade.php needed no changes
(f) any guest-menu line that revealed an unexpected sibling property worth noting for Phase 14
</output>
