# Phase 10.5: Brand Color Migration — Discussion Log

**Date:** 2026-04-30
**Mode:** discuss (default, interactive)
**Areas selected:** Token remap strategy, Body background treatment, Admin layout vs shop branding, Hardcoded color cleanup scope

---

## Area 1: Token Remap Strategy

### Q1.1 — Migration architecture
**Question:** How should the 10 legacy platform tokens get migrated to Bite greens?
**Options presented:**
- In-place value swap (Recommended) — keep names, change values, all 85 component references auto-inherit
- New `--brand-*` tokens, leave legacy alone — bigger refactor, components touched per chrome screen
- Hybrid: only `--crema` rebinds — lowest disruption, doesn't reach 60/30/10 dominance

**Selected:** In-place value swap (Recommended) → **D-01**

### Q1.2 — Color role distribution
**Question:** What does `--crema` (the energy color) become?
**Options presented:**
- Forest hierarchy (Recommended) — `--crema` = `#004225`, `--ink` near-black, hover = `#37B34A`
- `--crema` = vibrant green `#37B34A` — primary `#004225` only on dedicated surfaces
- `--crema` = secondary green `#0B6B2E` — neutral mid-forest

**Selected:** Forest hierarchy (Recommended) → **D-02**

### Q1.3 — Paper tint
**Question:** What tint does the new platform paper take?
**Options presented:**
- Pure white `#FFFFFF` — clinical
- Off-white `#FAFAF7` (Recommended) — slightly warm, paper feel
- Green-tinted `#F2F7F2` — visibly green territory

**Selected:** Off-white `#FAFAF7` (Recommended) → **D-03**

### Q1.4a — Status tokens
**Question:** How should `--signal` and `--focus` get remapped?
**Options presented:**
- Single brand-green for both (Recommended) — `--signal` = `--focus` = `#37B34A`
- Split: `--signal` = `#37B34A`, `--focus` = `#7AC70C`
- Keep `--signal` teal, only `--focus` rebinds

**Selected:** Single brand-green for both (Recommended) → **D-04**

### Q1.4b — Lime greens
**Question:** What do `#7AC70C` and `#B7C40D` do in v1.3?
**Options presented:**
- Reserved — tokens defined, no chrome use yet (Recommended)
- Welcome page only
- Use `--brand-accent-2` for status badges

**Selected:** Reserved (Recommended) → **D-05**

### Q1.5 — Continue or move on?
**Question:** Last token-remap questions, or move to next area?
**Selected:** More — derived neutrals

### Q1.6a — Derived neutrals
**Question:** How should `--canvas`, `--panel`, `--panel-muted`, `--line`, `--ink-soft` get values?
**Options presented:**
- Linear-interp from `--paper`/`--ink` (Recommended) — same algorithm as `app.blade.php` lines 56–72
- Hand-tuned green-leaning grays
- Pure neutral grays (Tailwind scale)

**Selected:** Linear-interp from paper/ink (Recommended) → **D-06**

### Q1.6b — `--brand-secondary` role
**Question:** What role does `#0B6B2E` play in 10.5?
**Options presented:**
- Defined, reserved (Recommended)
- `btn-primary` normal state
- Sidebar / navigation chrome

**Selected:** Defined, reserved (Recommended) → **D-07**

---

## Area 2: Body Background Treatment

### Q2.1 — Body background
**Question:** What does the platform-chrome body background become?
**Options presented:**
- Drop gradient — solid paper (Recommended) — `body { background: rgb(var(--paper)); }`, keep noise dots
- Keep gradient — retune to greens — preserves soft texture, faint green wash
- Bold — solid `#004225` body — dark forest, white panels float

**Selected:** Drop gradient — solid paper (Recommended) → **D-08, D-09**

### Q2.2 — Background scope
**Question:** Apply gradient drop globally or only platform chrome?
**Options presented:**
- Global — drop everywhere (Recommended) — tenants still warm because shop branding overrides `--paper`
- Platform only — tenant keeps gradient
- Both keep solid, drop body::before too

**Selected:** Global — drop everywhere (Recommended) → **D-10**

### Q2.3 — Continue or move on?
**Question:** More questions on body background, or move to admin chrome?
**Selected:** Next area — card differentiation goes to Claude's discretion (Recommended)

---

## Area 3: Admin Layout vs Shop Branding

### Q3.1 — Admin shop-branding injection
**Question:** Should `admin.blade.php` stop injecting shop branding so admin always shows Bite green?
**Options presented:**
- Strip injection — admin always Bite green (Recommended) — scoped preview on shop-settings
- Keep injection only on shop-settings — route check in @if
- Layered — chrome green, content respects shop

**Selected:** Strip injection — admin always Bite green (Recommended) → **D-11, D-12, D-13, D-14**

### Q3.2 — Override regression test
**Question:** What form should plan 10.5-02's regression test take?
**Options presented:**
- Feature test asserting `<style>` presence (Recommended) — Laravel feature test
- Computed-style browser test (Pest + Dusk) — most rigorous
- Manual visual checklist + grep guard — lowest rigor

**Selected:** Feature test asserting `<style>` presence (Recommended) → **D-15**

### Q3.3 — Continue or move on?
**Question:** More questions on admin chrome, or move to final area?
**Selected:** Next area — scoped-preview UI design is Claude's discretion (Recommended)

---

## Area 4: Hardcoded Color Cleanup Scope

### Q4.1 — Cleanup aggressiveness
**Question:** How aggressive is the hardcoded-color cleanup in Phase 10.5?
**Options presented:**
- Platform chrome only (Recommended) — 4 theme-color metas, login gradient, admin fallback values
- Minimal — `:root` + theme-color only
- Aggressive — sweep welcome page SVGs and dashboard shadows too

**Selected:** Platform chrome only (Recommended) → **D-16, D-17**

### Q4.2 — Wrap up
**Question:** Lock the verification grep now or wrap and write CONTEXT.md?
**Options presented:**
- Wrap and write CONTEXT.md (Recommended) — verification grep is Claude's discretion
- Lock the verification grep now
- Explore another gray area

**Selected:** Wrap and write CONTEXT.md (Recommended) → **D-18 (Claude's discretion), D-19 follows ROADMAP**

---

## Deferred Ideas Captured

- Welcome landing page color sweep → Phase 14
- Email template color migration → Phase 13
- Print template color migration → Phase 13
- Dashboard inline shadows + reports-dashboard chart colors → Phase 14
- `offline.blade.php` hardcoded values → Phase 14
- Roles for `--brand-accent-2` and `--brand-accent-3` in chrome → v1.4 backlog
- Role for `--brand-secondary` in dark theme / hover hierarchies → Phase 12
- WCAG AA contrast audit on new palette → separate milestone

## Claude's Discretion (left flexible for planner)

- Exact hex resolution of `--canvas`/`--panel`/`--panel-muted`/`--line`/`--ink-soft` (D-06 algorithm; static hex vs computed)
- Card-vs-body differentiation when both `--paper` and `--panel` resolve to near-white
- Exact UI shape of the scoped preview on `/admin/shop-settings`
- Whether `guest.blade.php` login gradient gets retuned or simplified to solid `--paper`
- Verification grep output format (Markdown table, plain count, grouped-by-file)
- Order of operations within the sweep

---

*Generated: 2026-04-30*
