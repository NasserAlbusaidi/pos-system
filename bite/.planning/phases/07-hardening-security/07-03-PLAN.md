---
phase: 07-hardening-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Livewire/GuestMenu.php
  - app/Livewire/Guest/OrderTracker.php
  - app/Livewire/BillingSettings.php
  - app/Livewire/KitchenDisplay.php
  - app/Livewire/ShopDashboard.php
  - tests/Feature/Livewire/PosDashboardTenantIsolationTest.php
  - tests/Feature/Livewire/KitchenDisplayTenantIsolationTest.php
  - tests/Feature/Livewire/ModifierManagerTenantIsolationTest.php
  - tests/Feature/Livewire/ReportsDashboardTenantIsolationTest.php
  - tests/Feature/Livewire/OrderTrackerValidationTest.php
  - tests/Feature/Livewire/InputValidationSweepTest.php
autonomous: true
requirements: [SEC-01, SEC-03]

must_haves:
  truths:
    - "PosDashboard cannot access or mutate orders belonging to a different shop"
    - "KitchenDisplay cannot access or transition orders belonging to a different shop"
    - "ModifierManager cannot create, update, or delete modifier groups belonging to a different shop"
    - "ReportsDashboard cannot query sales data from a different shop"
    - "OrderTracker validates feedback rating (1-5) and comment length before submission"
    - "All Livewire components that accept user input have validation rules applied"
  artifacts:
    - path: "tests/Feature/Livewire/PosDashboardTenantIsolationTest.php"
      provides: "Cross-tenant isolation regression test for POS"
      min_lines: 30
    - path: "tests/Feature/Livewire/KitchenDisplayTenantIsolationTest.php"
      provides: "Cross-tenant isolation regression test for KDS"
      min_lines: 30
    - path: "tests/Feature/Livewire/ModifierManagerTenantIsolationTest.php"
      provides: "Cross-tenant isolation regression test for modifier management"
      min_lines: 30
    - path: "tests/Feature/Livewire/ReportsDashboardTenantIsolationTest.php"
      provides: "Cross-tenant isolation regression test for reports"
      min_lines: 20
    - path: "tests/Feature/Livewire/OrderTrackerValidationTest.php"
      provides: "Input validation test for order tracker feedback"
      min_lines: 20
    - path: "tests/Feature/Livewire/InputValidationSweepTest.php"
      provides: "Validation coverage verification across all Livewire components"
      min_lines: 30
  key_links:
    - from: "tests/Feature/Livewire/PosDashboardTenantIsolationTest.php"
      to: "app/Livewire/PosDashboard.php"
      via: "Livewire::test(PosDashboard::class) with cross-tenant order"
      pattern: "Livewire::.*test.*PosDashboard"
    - from: "tests/Feature/Livewire/KitchenDisplayTenantIsolationTest.php"
      to: "app/Livewire/KitchenDisplay.php"
      via: "Livewire::test(KitchenDisplay::class) with cross-tenant order"
      pattern: "Livewire::.*test.*KitchenDisplay"
---

<objective>
Audit tenant isolation with regression tests for every tenant-scoped component, and sweep all Livewire components for input validation gaps.

Purpose: SEC-01 ensures no cross-tenant data leakage is possible — every gap found gets fixed and a regression test prevents future regressions. SEC-03 ensures all user inputs are validated against injection and XSS, including admin-facing components (D-16).
Output: 4 tenant isolation test files, validation fixes for components with gaps, validation sweep test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-security/07-CONTEXT.md
@.planning/phases/07-hardening-security/07-RESEARCH.md

<interfaces>
<!-- Existing test patterns and tenant-scoped models the executor needs -->

From tests/Feature/Livewire/MenuBuilderTenantIsolationTest.php (REFERENCE PATTERN for SEC-01):
```php
class MenuBuilderTenantIsolationTest extends TestCase
{
    use RefreshDatabase;

    public function test_cannot_assign_product_to_category_from_another_shop(): void
    {
        [$shopA, $shopB] = $this->makeShops();
        // ... create data in shopB, auth as shopA user, attempt cross-tenant mutation
        // Assert it's blocked
    }

    private function makeShops(): array
    {
        $shopA = Shop::create(['name' => 'Shop A', 'slug' => 'shop-a']);
        $shopB = Shop::create(['name' => 'Shop B', 'slug' => 'shop-b']);
        return [$shopA, $shopB];
    }
}
```

Tenant-scoped models (audit targets from CONTEXT.md):
- Product (shop_id, guarded)
- Order (shop_id, guarded) — used by PosDashboard, KitchenDisplay
- User (shop_id, guarded)
- Category (shop_id)
- ModifierGroup (shop_id) — used by ModifierManager
- PricingRule (shop_id)
- GroupCart (shop_id)
- LoyaltyCustomer (shop_id)
- AuditLog (shop_id)

Components needing cross-tenant tests (from research):
- PosDashboard — uses Auth::user()->shop_id in all queries
- KitchenDisplay — uses Auth::user()->shop_id
- ReportsDashboard — uses $shopId = Auth::user()->shop_id inline
- ModifierManager — uses Auth::user()->shop_id

Already tested:
- MenuBuilder — MenuBuilderTenantIsolationTest
- GuestMenu — GuestMenuSecurityTest (price manipulation)
- User model — UserTenancyTest

Livewire components with existing validation (review for SEC-03):
- OnboardingWizard, ShopSettings, ProductManager, ModifierManager, ShiftReport, CashReconciliation

Components that may need validation added (from research):
- OrderTracker — feedback form (rating, comment) needs validation
- BillingSettings — plan key validation inline but may have gaps
- GuestMenu — already has extensive validation
- KitchenDisplay — no user input besides order transitions (status enum)
- ShopDashboard — read-only dashboard, no user input
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tenant isolation regression tests for PosDashboard, KitchenDisplay, ModifierManager, and ReportsDashboard</name>
  <files>tests/Feature/Livewire/PosDashboardTenantIsolationTest.php, tests/Feature/Livewire/KitchenDisplayTenantIsolationTest.php, tests/Feature/Livewire/ModifierManagerTenantIsolationTest.php, tests/Feature/Livewire/ReportsDashboardTenantIsolationTest.php</files>
  <read_first>
    - tests/Feature/Livewire/MenuBuilderTenantIsolationTest.php (REFERENCE PATTERN — replicate this structure exactly)
    - app/Livewire/PosDashboard.php (find all methods that query orders — markPaid, markPreparing, markReady, markCompleted, cancelOrder etc. — verify each scopes by Auth::user()->shop_id)
    - app/Livewire/KitchenDisplay.php (find all methods that query/transition orders — verify shop_id scoping)
    - app/Livewire/ModifierManager.php (find all methods that create/update/delete modifier groups — verify shop_id scoping)
    - app/Livewire/Admin/ReportsDashboard.php (find all methods that query sales/order data — verify $shopId = Auth::user()->shop_id)
    - app/Models/Order.php (understand Order fields: shop_id, status, total_amount, tracking_token)
    - app/Models/ModifierGroup.php (understand ModifierGroup fields: shop_id, name_en, name_ar)
    - app/Models/Shop.php (understand Shop factory/creation pattern)
    - app/Models/User.php (understand User factory — need role field for different access levels)
  </read_first>
  <behavior>
    PosDashboard:
    - Test 1: User from shopA cannot mark shopB's order as paid (markPaid with shopB order ID returns no effect or 404)
    - Test 2: User from shopA cannot cancel shopB's order
    - Test 3: PosDashboard only lists orders from the authenticated user's shop

    KitchenDisplay:
    - Test 4: User from shopA cannot transition shopB's order to "preparing"
    - Test 5: User from shopA cannot transition shopB's order to "ready"
    - Test 6: KitchenDisplay only shows orders from the authenticated user's shop

    ModifierManager:
    - Test 7: User from shopA cannot update a modifier group belonging to shopB
    - Test 8: User from shopA cannot delete a modifier group belonging to shopB

    ReportsDashboard:
    - Test 9: ReportsDashboard for shopA user does not include shopB's order totals in sales data
  </behavior>
  <action>
    Create four test files following the EXACT pattern from `MenuBuilderTenantIsolationTest.php`:

    **Common helper pattern (replicate in each test file):**
    ```php
    use App\Models\Shop;
    use App\Models\User;
    use App\Models\Order;
    use Illuminate\Foundation\Testing\RefreshDatabase;
    use Livewire\Livewire;
    use Tests\TestCase;

    private function makeShops(): array
    {
        $shopA = Shop::create(['name' => 'Shop A', 'slug' => 'shop-a']);
        $shopB = Shop::create(['name' => 'Shop B', 'slug' => 'shop-b']);
        return [$shopA, $shopB];
    }
    ```

    **1. `tests/Feature/Livewire/PosDashboardTenantIsolationTest.php`**

    Test that calling `markPaid($orderBId, 'cash')` on PosDashboard while authenticated as shopA staff does NOT mark shopB's order as paid. Create an order in shopB using `Order::forceCreate(['shop_id' => $shopB->id, 'status' => 'unpaid', 'total_amount' => 10.000, 'tracking_token' => (string) \Illuminate\Support\Str::uuid()])`. Auth as shopA user with `role => 'server'`. After calling `markPaid`, assert shopB's order is still `unpaid` via `$orderB->fresh()->status`.

    Also test that `cancelOrder($orderBId)` does not cancel shopB's order.

    Also test that the PosDashboard component, when rendered for shopA user, does NOT show shopB's orders in the rendered output.

    **2. `tests/Feature/Livewire/KitchenDisplayTenantIsolationTest.php`**

    Similar pattern: create an order in shopB with status `paid`, authenticate as shopA kitchen user, call the method that transitions to `preparing` (read the component to find the exact method name — likely `updateStatus` or `transitionOrder`). Assert shopB's order status is unchanged.

    **3. `tests/Feature/Livewire/ModifierManagerTenantIsolationTest.php`**

    Create a ModifierGroup in shopB. Auth as shopA admin. Attempt to update and delete shopB's modifier group via the component's methods. Assert the modifier group is unchanged/still exists.

    Import `App\Models\ModifierGroup`. The ModifierGroup model needs `shop_id`, `name_en`, `name_ar` at minimum.

    **4. `tests/Feature/Livewire/ReportsDashboardTenantIsolationTest.php`**

    Create completed orders in both shopA and shopB. Auth as shopA manager. Mount ReportsDashboard component. Assert that the component's computed/displayed sales data only includes shopA's orders.

    Read the ReportsDashboard component carefully to understand what data it exposes (likely computed properties for sales totals, order counts, etc.) and write assertions against those specific properties.

    **IMPORTANT per D-14:** If any test FAILS (revealing a tenant isolation gap), FIX the gap in the component code. The audit is a pre-deploy gate. Do NOT leave failing tests. If a component allows cross-tenant access, add a `where('shop_id', Auth::user()->shop_id)` scope to the offending query.

    **IMPORTANT for test stability:** Each test should use `RefreshDatabase` trait. Orders need `tracking_token` (UUID, auto-generated on creation for real orders — but `forceCreate` may bypass the boot method, so set it explicitly). Use `Str::uuid()` for tracking tokens.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/POS/backend/bite && php artisan test --filter="PosDashboardTenantIsolationTest|KitchenDisplayTenantIsolationTest|ModifierManagerTenantIsolationTest|ReportsDashboardTenantIsolationTest"</automated>
  </verify>
  <acceptance_criteria>
    - tests/Feature/Livewire/PosDashboardTenantIsolationTest.php exists and contains at least 2 test methods
    - tests/Feature/Livewire/KitchenDisplayTenantIsolationTest.php exists and contains at least 2 test methods
    - tests/Feature/Livewire/ModifierManagerTenantIsolationTest.php exists and contains at least 2 test methods
    - tests/Feature/Livewire/ReportsDashboardTenantIsolationTest.php exists and contains at least 1 test method
    - Each test file contains `use RefreshDatabase` trait
    - Each test file contains `private function makeShops(): array`
    - All tenant isolation tests pass: `php artisan test --filter="TenantIsolationTest"` exits 0
    - If any component had a gap: the component file contains the fix AND a comment referencing SEC-01
    - `php artisan test` (full suite) exits 0
  </acceptance_criteria>
  <done>Every tenant-scoped Livewire component has regression tests verifying cross-tenant access is blocked. Any gaps found during audit are fixed. Tests serve as a pre-deploy gate per D-14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Input validation sweep across all Livewire components</name>
  <files>app/Livewire/Guest/OrderTracker.php, app/Livewire/BillingSettings.php, app/Livewire/GuestMenu.php, app/Livewire/KitchenDisplay.php, app/Livewire/ShopDashboard.php, tests/Feature/Livewire/OrderTrackerValidationTest.php, tests/Feature/Livewire/InputValidationSweepTest.php</files>
  <read_first>
    - app/Livewire/Guest/OrderTracker.php (check for feedback/rating methods — does it accept user input? What validation exists?)
    - app/Livewire/BillingSettings.php (check subscribe() method — plan key validation, any other inputs?)
    - app/Livewire/GuestMenu.php (review submitOrder validation — already has extensive validation, but check for XSS in notes/comments)
    - app/Livewire/KitchenDisplay.php (check for user inputs beyond order transitions)
    - app/Livewire/ShopDashboard.php (check for user inputs — likely read-only)
    - app/Livewire/OnboardingWizard.php (reference — already has validation, check pattern)
    - app/Livewire/ShopSettings.php (reference — already has validation, check pattern)
    - app/Livewire/ProductManager.php (reference — already has validation, check pattern)
    - app/Livewire/ModifierManager.php (reference — already has validation, check pattern)
    - app/Livewire/ShiftReport.php (reference — already has validation, check pattern)
    - app/Livewire/CashReconciliation.php (reference — already has validation, check pattern)
    - app/Livewire/PinLogin.php (reference — already has validation + rate limiting)
  </read_first>
  <behavior>
    OrderTracker:
    - Test 1: Feedback rating must be integer between 1-5 (submitting 0 or 6 is rejected)
    - Test 2: Feedback comment must be string with max 500 characters
    - Test 3: Submitting feedback with XSS payload in comment is sanitized or rejected

    BillingSettings:
    - Test 4: subscribe() rejects invalid plan keys (not in allowlist)

    InputValidationSweep:
    - Test 5: GuestMenu order notes are validated (max length, no script tags)
    - Test 6: Every Livewire component that has public writable properties also has validation rules
  </behavior>
  <action>
    **Phase 1: Audit all 16 Livewire components for validation gaps**

    Read each component file listed in `read_first`. For each component, identify:
    1. Public properties that accept user input
    2. Methods callable from the frontend (public methods, not lifecycle)
    3. Whether `$this->validate()` or `$this->validateOnly()` is called before data mutations
    4. Whether file uploads have type/size validation

    **Expected findings based on research:**
    - Components WITH validation (review, don't rewrite): OnboardingWizard, ShopSettings, ProductManager, ModifierManager, ShiftReport, CashReconciliation
    - Components likely needing validation additions: OrderTracker (feedback form), possibly BillingSettings
    - Components with no user input (skip): ShopDashboard (read-only), KitchenDisplay (order transitions only — status is enum, not free text)

    **Phase 2: Fix validation gaps**

    **OrderTracker** — If `OrderTracker` has a feedback submission method (e.g., `submitFeedback`, `submitRating`), add validation rules:
    ```php
    $this->validate([
        'rating' => ['required', 'integer', 'min:1', 'max:5'],
        'comment' => ['nullable', 'string', 'max:500'],
    ]);
    ```

    If `OrderTracker` stores feedback, ensure the comment is sanitized with `strip_tags()` or Laravel's `e()` helper before storage to prevent stored XSS.

    **GuestMenu** — Check that `orderNotes` (if it exists as a public property) has a max length validation and is sanitized. The research says GuestMenu already has extensive validation, but verify the notes/comments field specifically.

    **BillingSettings** — Verify the `subscribe()` method validates the plan key against an allowlist. The research says this is already done inline. If it uses a hardcoded `in_array()` check, that's fine — document in the sweep test.

    **Phase 3: Create tests**

    **1. `tests/Feature/Livewire/OrderTrackerValidationTest.php`**

    Test feedback validation:
    - Submit feedback with rating=0 — assert validation error on 'rating'
    - Submit feedback with rating=6 — assert validation error on 'rating'
    - Submit feedback with comment of 501+ characters — assert validation error on 'comment'
    - Submit feedback with valid rating=5 and comment="Great!" — assert success
    - Submit feedback with `<script>alert('xss')</script>` in comment — assert the stored value does NOT contain `<script>`

    NOTE: Read OrderTracker first to understand the exact method name and properties. If there is no feedback feature, skip this test and document in the sweep test that OrderTracker has no user-mutable state.

    **2. `tests/Feature/Livewire/InputValidationSweepTest.php`**

    A meta-test that verifies all Livewire components with user-writable properties have validation:
    - For each component known to accept input, instantiate it and verify that calling a mutation method with invalid data produces validation errors (not unhandled exceptions)
    - Assert that components like GuestMenu validate order notes length
    - This is a comprehensive sweep test per D-16 covering all components

    Structure as one test per component group:
    - `test_onboarding_wizard_validates_shop_name()` — empty name is rejected
    - `test_product_manager_validates_product_fields()` — empty product name is rejected
    - `test_guest_menu_validates_order_notes_length()` — notes over max length rejected (if applicable)
    - Keep these lightweight — they verify validation EXISTS, not every edge case

    **IMPORTANT per D-16:** The sweep covers ALL Livewire components, not just public-facing ones. Admin inputs can be stored XSS or injection vectors.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/POS/backend/bite && php artisan test --filter="OrderTrackerValidationTest|InputValidationSweepTest"</automated>
  </verify>
  <acceptance_criteria>
    - tests/Feature/Livewire/OrderTrackerValidationTest.php exists (unless OrderTracker has no feedback feature — in which case document in sweep test)
    - tests/Feature/Livewire/InputValidationSweepTest.php exists and contains at least 3 test methods
    - If OrderTracker has a feedback method: app/Livewire/Guest/OrderTracker.php contains validation rules for rating and comment
    - If OrderTracker stores comments: comment field is sanitized (strip_tags or equivalent)
    - Every Livewire component with user input has `$this->validate()` or equivalent
    - `php artisan test --filter="OrderTrackerValidationTest|InputValidationSweepTest"` exits 0
    - `php artisan test` (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>All Livewire components audited for input validation. Gaps in OrderTracker (and any others found) are fixed with proper validation rules. Sweep test verifies validation exists across all input-accepting components. D-16 satisfied.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `php artisan test --filter="TenantIsolationTest"` — all cross-tenant tests pass
2. `php artisan test --filter="ValidationTest|ValidationSweepTest"` — all validation tests pass
3. `php artisan test` — full suite green, zero regressions
4. Every tenant-scoped component (PosDashboard, KitchenDisplay, ModifierManager, ReportsDashboard) has a regression test
5. Every Livewire component with user input has validation rules applied
</verification>

<success_criteria>
- 4 new tenant isolation test files covering PosDashboard, KitchenDisplay, ModifierManager, ReportsDashboard (SEC-01)
- Any tenant isolation gaps found are FIXED in component code (D-14: pre-deploy gate)
- Regression tests prevent future tenant isolation breaks (D-15)
- All Livewire components with user input have validation (SEC-03, D-16)
- OrderTracker feedback validated (rating 1-5, comment max 500, XSS sanitized)
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-security/07-03-SUMMARY.md`
</output>
